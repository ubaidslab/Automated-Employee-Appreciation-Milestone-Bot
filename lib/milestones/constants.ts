/**
 * Which years of service get celebrated. Deliberately not "every year" —
 * a message every single year, every employee, is noise that kills adoption
 * of tools like this in real teams.
 */
export const DEFAULT_MILESTONE_YEARS = [1, 2, 3, 5, 10, 15, 20, 25, 30];

export const DEFAULT_UPCOMING_WINDOW_DAYS = 30;
